// Register project-specific Markdown extensions here in execution order.
// GitBook compatibility is enabled by default; ordinary Markdown needs no plugin.
export const markdownOptions = {
  gitbook: true,
  extensions: [],
}
